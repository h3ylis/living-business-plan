{
  "name": "exec",
  "version": "1.0.0",
  "description": "Living business plan — reads like a document, edits like a conversation, decides like a board vote",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.0",
    "marked": "^15.0.0",
    "nodemailer": "^7.0.0",
    "dotenv": "^16.5.0"
  }
}
